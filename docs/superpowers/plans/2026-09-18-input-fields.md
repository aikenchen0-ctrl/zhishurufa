# 输入场景与宿主隔离

目标：数字、电话、邮箱、密码、URI、日期时间输入框使用匹配的键盘，离开临时输入场景后恢复原中文方案与模式；SDK 不占用宿主通用存储。

职责：纯策略负责字段分类及临时状态；KeyboardWindow 负责应用决策；YAML 定义键位；Rime 继续负责文字转换。宿主动作保持原有独立接口。

- [x] 检查字段路由与回退逻辑。
- [x] 编写分类、跨字段状态保留和回退测试。
- [x] 增加电话、邮箱布局，修复字段切换与模式恢复。
- [x] 修复只读审计发现的明确宿主存储冲突。
- [x] 增加设备测试：中文到数字、电话、邮箱、密码，再返回中文。
- [x] 执行单元、设备输入和宿主集成验证。
- [x] 重建 SDK 与独立 App，校验产物。
- [x] 更新结果及限制。

设备级多字段切换用例已纳入验收：覆盖中文、数字、电话、邮箱、密码和英文偏好恢复；字段分类、URI 路由、临时状态清理和嵌套状态由单元测试覆盖，Release x86_64 与 arm64-v8a 转译各十四组设备测试通过。
